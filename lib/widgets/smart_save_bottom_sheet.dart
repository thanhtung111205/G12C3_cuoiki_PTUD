import 'package:flutter/material.dart';

class SmartSaveBottomSheet extends StatelessWidget {
  const SmartSaveBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 200,
      child: Center(
        child: Text('Smart Save Bottom Sheet'),
      ),
    );
  }
}